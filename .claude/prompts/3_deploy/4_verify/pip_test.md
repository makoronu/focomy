# pip インストールテスト

## やること
1. 新規venv作成
2. pip install focomy
3. focomy --version 確認
4. focomy init テスト

## コマンド
```bash
python -m venv /tmp/focomy-verify
source /tmp/focomy-verify/bin/activate
pip install focomy
focomy --version
focomy init testsite
```

## 完了条件
- [ ] インストール成功
- [ ] CLIコマンド動作

## 次 → log.md
