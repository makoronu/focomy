# プロジェクト構造確認

## やること
1. ディレクトリ構造確認
2. 設定ファイル確認
3. ドキュメント確認

## コマンド
```bash
find . -type f \( -name "*.py" -o -name "*.tsx" \) | head -50
ls -la *.json *.yaml .env* 2>/dev/null
find . -name "*.md" -type f
```

## 完了条件
- [ ] 構造を把握した

## 次の工程
→ screenshot.md
